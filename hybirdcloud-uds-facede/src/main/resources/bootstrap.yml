spring:
  cloud:
    config:
      name: hybirdcloud-uds-facede,database
      profile: dev
      discovery:
        enabled: true
        service-id: hybirdcloud-uds-config
  application:
    name: hybirdcloud-uds-facede
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8801/eureka/